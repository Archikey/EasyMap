name: Publish NuGet

on:
  push:
    branches:
      - master
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore library
        run: dotnet restore EasyMapFolder/EasyMap/EasyMap.csproj

      - name: Restore tests
        run: dotnet restore EasyMapFolder/TestEasyMap/TestEasyMap.csproj

      - name: Build library
        run: dotnet build EasyMapFolder/EasyMap/EasyMap.csproj -c Release --no-restore

      - name: Build tests
        run: dotnet build EasyMapFolder/TestEasyMap/TestEasyMap.csproj -c Release --no-restore

      - name: Run tests
        run: dotnet test EasyMapFolder/TestEasyMap/TestEasyMap.csproj -c Release --no-build

      - name: Pack
        if: startsWith(github.ref, 'refs/tags/v')
        run: dotnet pack EasyMapFolder/EasyMap/EasyMap.csproj -c Release --no-build -o ./artifacts

      - name: Publish
        if: startsWith(github.ref, 'refs/tags/v')
        run: dotnet nuget push "./artifacts/*.nupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate